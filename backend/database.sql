CREATE DATABASE IF NOT EXISTS expense - app;
USE expense - app;
-- Sequelize will create/update the users table with:
-- id, name, email, password, createdAt, updatedAt
-- You only need to create the database manually.